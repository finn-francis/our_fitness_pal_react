import React from 'react'

const FullScreenModalFooter = (props) => {
  return (
    <div className="modal-footer-full-width modal-footer text-center">
      {props.children}
    </div>
  )
}

export default FullScreenModalFooter
